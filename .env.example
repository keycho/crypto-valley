# PostgreSQL connection string (matches docker-compose.yml).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/crypto_valley

# Fastify API server — exposes GET /health.
API_PORT=3001

# Realtime game server (ws).
GAME_SERVER_PORT=8080
